# WireGuard Configuration Manager - Environment Variables
# Copy this file to .env and fill in the values

# ============================================================================
# Supabase Configuration
# ============================================================================
# For local development: Use local Supabase instance (npx supabase start)
# For production: Get these from your Supabase project settings at:
# https://app.supabase.com/project/_/settings/api

# Supabase project URL
# Local: http://localhost:54321 (default from supabase start)
# Production: https://your-project.supabase.co
SUPABASE_URL=http://localhost:54321

# Supabase anonymous (public) key
# Local: Use the anon key from `npx supabase start` output
# Production: Get from Supabase project settings
SUPABASE_ANON_KEY=

# Supabase service role key (for admin operations)
# ⚠️ WARNING: Keep this secret! Never expose to client-side code
# Local: Use the service_role key from `npx supabase start` output
# Production: Get from Supabase project settings
SUPABASE_SERVICE_ROLE_KEY=

# ============================================================================
# Import Configuration
# ============================================================================

# Directory path where WireGuard .conf files are stored for import
# This directory will be scanned by the import endpoint
# Example: /var/wg-import or /opt/wireguard/configs
IMPORT_DIR=/path/to/wireguard/configs

# ============================================================================
# Encryption Configuration
# ============================================================================

# AES-256 encryption key for WireGuard configuration files (64 hex characters = 32 bytes)
# Generate using: openssl rand -hex 32
# ⚠️ WARNING: Keep this secret! If lost, configs cannot be decrypted
ENCRYPTION_KEY=your-64-character-hex-key-here

# ============================================================================
# Application Configuration
# ============================================================================

# Node environment (development, production)
NODE_ENV=development

# Application port (optional, defaults to 4321 for Astro)
# PORT=4321

# Email domains allowed for user registration (comma-separated)
# Example: example.com,mycompany.com,*.contractor.com
# Supports wildcards (*.subdomain.com) for all subdomains
ACCEPTED_DOMAINS=example.com

# ============================================================================
# Cloudflare Tunnel Configuration (Optional - for production deployment)
# ============================================================================

# Cloudflare Tunnel token
# Get this from: https://one.dash.cloudflare.com/ → Zero Trust → Networks → Tunnels
# Create a tunnel and copy the token
# CLOUDFLARE_TUNNEL_TOKEN=

